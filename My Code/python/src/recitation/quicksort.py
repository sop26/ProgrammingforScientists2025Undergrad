# loop through all
    # store first elem
    # loop through all elements after
        # if element smaller than first, add to array 1.
        # if element is bigger, add to array 2
    # loop through array 1
        # store first elem
        #f element smaller than first, add to array a
        # if element is bigger, add to array b
    # loo through array 2
# return array 1a - first elem, array 1b, first elem, array  2a - first elem, array 2b


# def quick_sort (unsorted_array):
#     if len(unsorted_array < 2):
#         return unsorted_array
#     pivot = unsorted_array[0]
#     less, greater = partition(pivot, unsorted_array)
#         quick_sort(less)
#         quick_sort(greater)